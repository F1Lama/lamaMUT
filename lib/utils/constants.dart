import 'package:flutter/material.dart';

class AppRoutes {
  static const String permissionRequests = '/PermissionRequests';
  static const String exitPermits = '/ExitPermits';
  static const String studentAttendance = '/StudentAttendance';
  static const String previousRequests = '/PreviousRequests';
}

class AppColors {
  static const Color primaryColor = Color.fromARGB(255, 1, 113, 189);
  static const Color secondaryColor = Colors.green;
}
